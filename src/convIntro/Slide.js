export class Slide {
    constructor(img, kernel, annotation, cell_x, cell_y) {
        this.img = img;
        this.kernel = kernel;
        this.annotation = annotation;
        this.cell_x = cell_x;
        this.cell_y = cell_y;
    }
}